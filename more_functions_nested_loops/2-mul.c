#include "main.h"
#include <stdio.h>

/**
 * mul - multiplies two integers
 * @a : varibale
 * @b : variable
 * Return: Always 0.
 */

int mul(int a, int b)

{
	int result;

	result = a * b;
	return (result);
}
