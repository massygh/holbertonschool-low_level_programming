#include <stdio.h>
#include "main.h"

/**
 * _isdigit - Write a function that checks for lowercase character.
 * @c: int
 * Return: 0 is succes
 */

void print_numbers(void);

{
	int a;

	for (a = '0' ; a <= '9'; a++)
		_putchar(a);
	_putchar('\n');
}
