#include <stdio.h>
#include "main.h"

/**
 * print_alphabet - use prototype
 * _islower - Write a function that checks for lowercase character.
 * @c - int
 */

int _islower(int c)

{
	{
	if (c >= 'a' && c <= 'z')
		return (1);
	else
		return (0);
	}
}
