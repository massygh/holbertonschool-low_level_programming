#include "main.h"
#include <stdio.h>

/**
 * main - check the code
 *
 * Return: Always 0.
 */

int print_last_digit(int)

	int a;
	int b;

	a = n;
	b = a % 10;
	result = "%d %d, a,z);
	putchar(result)
	return (b);
