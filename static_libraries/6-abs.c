#include "main.h"
#include <stdio.h>

/**
 * _abs - the absolute value of an integer
 * @n : main
 *
 * Return: Always 0.
 */

int _abs(int n)
{
	if (n < 0)
	{
	return (-n);
	}
	else
	{
	return (n);
	}
}
